<article class="page container">
  <header style="margin-bottom: 32px;">
    <p class="eyebrow">{{ page.template_suffix | default: 'Page' | replace: '-', ' ' }}</p>
    <h1>{{ page.title }}</h1>
  </header>
  <div class="page-content rte">
    {{ page.content }}
  </div>
</article>
